1-1 mapping for note integers. 
Build two structs, one for json from and to.
Need to capture name.

Traverse directory, convert to json, then pack fields in to the struct for keyswitch output
Capture current directory name, and create new top level directory for ableton json in the same structure
